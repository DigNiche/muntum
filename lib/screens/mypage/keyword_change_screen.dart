import 'package:flutter/material.dart';

class KeywordChangeScreen extends StatelessWidget {
  const KeywordChangeScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
